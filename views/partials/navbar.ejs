<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand mb-0 h1" href="/">Библиотечный фонд</a>
    <button
        class="navbar-toggler"
        type="button"
        data-toggle="collapse"
        data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent"
        aria-expanded="false"
        aria-label="Toggle navigation"
    >
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item <% if(active === 'main'){ %>active<% } %>">
                <a class="nav-link" href="/">Главная</a>
            </li>
            <li class="nav-item <% if(active === 'catalog'){ %>active<% } %>">
                <a class="nav-link" href="/catalog">Каталог</a>
            </li>
            <li class="nav-item <% if(active === 'user'){ %>active<% } %>">
                <a class="nav-link" href="/user">Личный кабинет</a>
            </li>
        </ul>
        <% try{ %> <% if(session == undefined) { %>
        <form class="form-inline my-2 my-lg-0" action="/login">
            <button
                class="btn btn-outline-success my-2 mr-sm-2 my-sm-0"
                type="submit"
            >
                Войти
            </button>
        </form>
        <form class="form-inline my-2 my-lg-0" action="/reg">
            <button class="btn btn-outline-success my-2 my-sm-0" type="submit">
                Зарегистрироваться
            </button>
        </form>
        <% } else { %>
        <form class="form-inline my-2 my-lg-0" action="/logout">
            <button
                class="btn btn-outline-warning my-2 mr-sm-2 my-sm-0"
                type="submit"
            >
                Выйти
            </button>
        </form>
        <% } %> <% }catch(e){ }%>
    </div>
</nav>
